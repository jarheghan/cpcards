<script type="text/javascript">
        function ToggleButton(ornumber,price) {
            $("#btn-" + ornumber.toString()).toggleClass("btn1 btn-large btn-primary").addClass("btn1 btn-large disable");
            if ($("#btn-" + ornumber.toString()).hasClass('btn-primary btn1 btn-large')) {
                $("#hid-" + ornumber).attr("value", ornumber);
                $("#ret-" + ornumber).attr("value", price);
                //$("#hid-" + ornumber).html("<input type='hidden' value=" + "'" + ornumber + "'" + " id=" + "'" + "hid-" + ornumber + "' />")
            }
            else {
                $("#hid-" + ornumber).removeAttr("value");
                $("#ret-" + ornumber).removeAttr("value");
                //$("#hid-" + ornumber).remove();
            }
        }
</script>
<script type="text/javascript">
    $("#flipRack").bind("click", function (event) {
        $("div#frontrack").toggle();
    });
</script>


      <script type="text/javascript">

          $("#checkUncheck").click(function () {
              GetRack();
              return false;
          });

          function GetRack() {

              var model = @Html.Raw(Json.Encode(Model.EDCards));
              var item = [];
              var par = $("#findCheck").val();
              $.each(model, function(key, val){
                  //item.push($("#btn-"+ val.Space).toggleClass("btn1 btn-large btn-primary").addClass("btn1 btn-large"));
                  //$("#btn-"+ val.Space).toggleClass("btn1 btn-large btn-primary").addClass("btn1 btn-large");
                  if(($("#btn-" + val.Space).hasClass('btn-primary btn1 btn-large') ||$("#btn-" + val.Space).hasClass('btn1 btn-large btn-primary')) && (par === "0"))
                  {
                      $("#btn-"+ val.Space).toggleClass("btn-primary btn1 btn-large").addClass("btn1 btn-large");
                      ($("#btn-"+ val.Space).removeClass("btn-primary btn1 btn-large").addClass("btn1 btn-large")||
                       $("#btn-"+ val.Space).removeClass("btn1 btn-large btn-primary").addClass("btn1 btn-large"));
                     // $("#findCheck").attr("value", "1");

                  }
                  
                  if($("#btn-" + val.Space).hasClass('btn1 btn-large') && (par === "1"))
                  {
                      $("#btn-"+ val.Space).toggleClass("btn1 btn-large").addClass("btn1 btn-large btn-primary");
                      $("#btn-"+ val.Space).removeClass("btn1 btn-large").addClass("btn1 btn-large btn-primary");
                      //$("#findCheck").attr("value", "0");
                  }
                  
                  if ($("#btn-" + val.Space).hasClass('btn-primary btn1 btn-large')) {
                      $("#hid-" + val.Space).attr("value", val.Space);
                      $("#ret-" + val.Space).attr("value", val.Space);
                      $("#findCheck").attr("value", "0");
                  }
                  else {
                      $("#hid-" + val.Space).removeAttr("value");
                      $("#ret-" + val.Space).removeAttr("value");
                      $("#findCheck").attr("value", "1");
                  }
              });
              

    }

</script>
<script type="text/javascript">
    $('#popup').ready(function(){
        toastr.options = {
            "debug": false,
            "positionClass": "toast-top-full-width",
            "onclick": null,
            "fadeIn": 300,
            "fadeOut": 1000,
            "timeOut": 5000,
            "extendedTimeOut": 1000
        };
        var ss = @Model.CosstValue.Completed;
        if(ss === 1)
        {
            toastr.success("Cards in Racks where successfully Inserted!","Insert Message");
        }
    });
</script>
